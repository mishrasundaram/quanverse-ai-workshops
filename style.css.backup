* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

:root {
  --bg: #02050a;
  --panel: rgba(9, 18, 32, 0.88);
  --panel-soft: rgba(14, 28, 48, 0.78);
  --text: #e6fbff;
  --muted: #8ea3b5;
  --cyan: #00f5ff;
  --green: #35ff8d;
  --red: #ff003c;
  --orange: #ff8a00;
  --purple: #8b5cf6;
  --border: rgba(0, 245, 255, 0.2);
  --danger-border: rgba(255, 0, 60, 0.3);
  --glow: 0 0 35px rgba(0, 245, 255, 0.18);
  --danger-glow: 0 0 35px rgba(255, 0, 60, 0.18);
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  background: var(--bg);
  color: var(--text);
  line-height: 1.6;
  overflow-x: hidden;
}

body::before {
  content: "";
  position: fixed;
  inset: 0;
  z-index: -5;
  background:
    radial-gradient(circle at 15% 10%, rgba(0,245,255,0.14), transparent 28%),
    radial-gradient(circle at 90% 20%, rgba(255,0,60,0.12), transparent 30%),
    radial-gradient(circle at 60% 90%, rgba(53,255,141,0.1), transparent 35%),
    linear-gradient(180deg, #02050a, #07111f 55%, #02050a);
}

.grid-bg {
  position: fixed;
  inset: 0;
  z-index: -4;
  opacity: 0.18;
  background-image:
    linear-gradient(rgba(0,245,255,0.14) 1px, transparent 1px),
    linear-gradient(90deg, rgba(0,245,255,0.14) 1px, transparent 1px);
  background-size: 44px 44px;
}

.red-alert {
  position: fixed;
  inset: 0;
  z-index: -3;
  pointer-events: none;
  background:
    radial-gradient(circle at 50% 15%, transparent 0%, transparent 35%, rgba(0,0,0,0.45) 100%),
    radial-gradient(circle at 85% 70%, rgba(255,0,60,0.08), transparent 32%);
}

.scanline {
  position: fixed;
  inset: 0;
  z-index: -2;
  pointer-events: none;
  background: repeating-linear-gradient(
    to bottom,
    rgba(255,255,255,0.025),
    rgba(255,255,255,0.025) 1px,
    transparent 1px,
    transparent 4px
  );
}

.ai-skull {
  position: fixed;
  right: -40px;
  top: 130px;
  font-size: 150px;
  font-weight: 1000;
  letter-spacing: -0.12em;
  color: rgba(255,0,60,0.08);
  text-shadow: 0 0 45px rgba(255,0,60,0.28);
  z-index: -1;
  transform: rotate(8deg);
  animation: skullPulse 3s infinite;
}

a {
  color: inherit;
  text-decoration: none;
}

.navbar {
  position: sticky;
  top: 0;
  z-index: 100;
  padding: 16px 6%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: rgba(2, 5, 10, 0.82);
  backdrop-filter: blur(18px);
  border-bottom: 1px solid var(--border);
}

.brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.brand-icon {
  width: 44px;
  height: 44px;
  display: grid;
  place-items: center;
  border-radius: 14px;
  color: #02050a;
  font-weight: 1000;
  background: linear-gradient(135deg, var(--cyan), var(--green));
  box-shadow: var(--glow);
}

.brand strong {
  display: block;
  font-weight: 900;
}

.brand span {
  display: block;
  font-size: 12px;
  color: var(--muted);
}

nav {
  display: flex;
  gap: 24px;
  align-items: center;
  font-size: 14px;
  font-weight: 800;
}

nav a {
  color: var(--muted);
  transition: 0.2s;
}

nav a:hover {
  color: var(--cyan);
  text-shadow: 0 0 14px var(--cyan);
}

.nav-btn {
  padding: 10px 16px;
  border-radius: 999px;
  color: #02050a !important;
  background: linear-gradient(135deg, var(--cyan), var(--green));
}

.menu-btn {
  display: none;
  border: 1px solid var(--border);
  background: var(--panel);
  color: var(--cyan);
  font-size: 24px;
  padding: 8px 12px;
  border-radius: 10px;
}

.hero {
  min-height: 88vh;
  padding: 96px 6%;
  display: grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap: 54px;
  align-items: center;
  position: relative;
}

.hero::before {
  content: "WARNING: UNSAFE AI USAGE DETECTED";
  position: absolute;
  top: 46px;
  left: 6%;
  color: rgba(255,0,60,0.34);
  font-size: 12px;
  font-weight: 1000;
  letter-spacing: 0.24em;
  animation: glitch 2.3s infinite;
}

.eyebrow {
  color: var(--green);
  text-transform: uppercase;
  font-weight: 1000;
  font-size: 12px;
  letter-spacing: 0.16em;
  margin-bottom: 14px;
}

h1 {
  font-size: clamp(42px, 6vw, 76px);
  line-height: 0.96;
  letter-spacing: -0.06em;
  margin-bottom: 24px;
  text-shadow:
    0 0 30px rgba(0,245,255,0.14),
    0 0 20px rgba(255,0,60,0.18);
}

.hero-text {
  max-width: 780px;
  font-size: 20px;
  color: var(--muted);
  margin-bottom: 32px;
}

.hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 14px;
  margin-bottom: 36px;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  padding: 14px 22px;
  border-radius: 999px;
  font-weight: 1000;
  transition: 0.22s;
}

.btn.primary {
  color: #02050a;
  background: linear-gradient(135deg, var(--cyan), var(--green));
  box-shadow: 0 0 35px rgba(0,245,255,0.2);
}

.btn.ghost {
  border: 1px solid var(--border);
  color: var(--cyan);
  background: rgba(9,18,32,0.72);
}

.btn:hover {
  transform: translateY(-3px);
  filter: brightness(1.12);
}

.trust-row {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14px;
  max-width: 680px;
}

.trust-row div {
  padding: 18px;
  border-radius: 18px;
  border: 1px solid var(--border);
  background: var(--panel);
  box-shadow: var(--glow);
}

.trust-row strong {
  display: block;
  color: var(--cyan);
  font-size: 22px;
}

.trust-row span {
  color: var(--muted);
  font-size: 13px;
}

.hero-right {
  position: relative;
}

.terminal-card {
  border-radius: 26px;
  overflow: hidden;
  background: rgba(2, 5, 10, 0.96);
  border: 1px solid var(--danger-border);
  box-shadow:
    0 0 50px rgba(0,245,255,0.12),
    0 0 42px rgba(255,0,60,0.14);
  transform: rotate(1.4deg);
}

.terminal-top {
  height: 50px;
  padding: 0 18px;
  display: flex;
  align-items: center;
  gap: 8px;
  background: rgba(12, 22, 36, 0.94);
  border-bottom: 1px solid var(--border);
}

.terminal-top span {
  width: 12px;
  height: 12px;
  border-radius: 50%;
}

.terminal-top span:nth-child(1) {
  background: var(--red);
  box-shadow: 0 0 12px var(--red);
}

.terminal-top span:nth-child(2) {
  background: var(--orange);
  box-shadow: 0 0 12px var(--orange);
}

.terminal-top span:nth-child(3) {
  background: var(--green);
  box-shadow: 0 0 12px var(--green);
}

.terminal-top p {
  margin-left: 12px;
  font-size: 13px;
  color: var(--muted);
  font-weight: 900;
}

pre {
  padding: 30px;
  white-space: pre-wrap;
  color: #caffdf;
  font-size: 16px;
  text-shadow: 0 0 10px rgba(53,255,141,0.22);
}

pre::after {
  content: "_";
  color: var(--green);
  animation: blink 0.8s infinite;
}

.floating-badge {
  position: absolute;
  padding: 10px 14px;
  border-radius: 14px;
  background: rgba(2,5,10,0.9);
  border: 1px solid var(--border);
  font-size: 12px;
  font-weight: 1000;
  box-shadow: var(--glow);
  animation: floatY 3.4s ease-in-out infinite;
}

.badge-1 {
  left: -30px;
  bottom: 70px;
  color: var(--green);
}

.badge-2 {
  right: -18px;
  top: 80px;
  color: var(--red);
  border-color: var(--danger-border);
  box-shadow: var(--danger-glow);
  animation-delay: 0.5s;
}

.badge-3 {
  left: 30px;
  top: -28px;
  color: var(--cyan);
  animation-delay: 1s;
}

.section {
  padding: 88px 6%;
  position: relative;
}

.section::before {
  content: "01000001 01001001 00100000 01000011 01011001 01000010 01000101 01010010";
  position: absolute;
  right: 6%;
  top: 22px;
  color: rgba(0,245,255,0.08);
  letter-spacing: 0.22em;
  font-size: 11px;
}

.section-head {
  max-width: 850px;
  margin-bottom: 42px;
}

.section h2,
.section-head h2 {
  font-size: clamp(32px, 4vw, 54px);
  line-height: 1.05;
  letter-spacing: -0.04em;
}

.section p,
.section-head p {
  color: var(--muted);
  font-size: 18px;
}

.warning {
  max-width: 1180px;
  margin: 0 auto;
  border: 1px solid var(--danger-border);
  border-radius: 28px;
  background:
    radial-gradient(circle at top right, rgba(255,0,60,0.14), transparent 35%),
    var(--panel);
  box-shadow: var(--danger-glow);
}

.warning h2 {
  margin-bottom: 18px;
}

.workshop-grid,
.skill-grid,
.demo-grid,
.audience-grid {
  display: grid;
  gap: 22px;
}

.workshop-grid {
  grid-template-columns: repeat(2, 1fr);
}

.workshop-card,
.skill-card,
.demo-box,
.timeline-item,
.audience-grid div {
  background: linear-gradient(180deg, rgba(12,22,36,0.9), rgba(2,5,10,0.76));
  border: 1px solid var(--border);
  border-radius: 24px;
  padding: 28px;
  box-shadow: 0 0 35px rgba(0,245,255,0.08);
  transition: 0.25s;
}

.workshop-card:hover,
.skill-card:hover,
.demo-box:hover,
.timeline-item:hover,
.audience-grid div:hover {
  transform: translateY(-6px);
  border-color: rgba(53,255,141,0.42);
  box-shadow:
    0 0 35px rgba(0,245,255,0.12),
    0 0 28px rgba(53,255,141,0.08);
}

.danger-card {
  border-color: var(--danger-border);
}

.date-pill {
  display: inline-block;
  padding: 7px 13px;
  border-radius: 999px;
  background: rgba(53,255,141,0.1);
  border: 1px solid rgba(53,255,141,0.28);
  color: var(--green);
  font-weight: 1000;
  margin-bottom: 16px;
}

.date-pill.red {
  background: rgba(255,0,60,0.1);
  border-color: rgba(255,0,60,0.3);
  color: var(--red);
}

.workshop-card h3 {
  font-size: 28px;
  margin-bottom: 14px;
}

.workshop-card ul {
  margin: 20px 0;
  padding-left: 20px;
  color: var(--muted);
}

.mini-link {
  color: var(--cyan);
  font-weight: 1000;
}

.dark-panel {
  background: rgba(2,5,10,0.55);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

.skill-grid {
  grid-template-columns: repeat(3, 1fr);
}

.skill-card span {
  color: var(--green);
  font-weight: 1000;
}

.skill-card h3 {
  font-size: 23px;
  margin: 10px 0;
}

.demo-grid {
  grid-template-columns: repeat(3, 1fr);
}

.demo-box {
  color: var(--cyan);
  font-weight: 900;
}

.timeline {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 22px;
}

.timeline-item span {
  display: inline-block;
  color: #02050a;
  background: linear-gradient(135deg, var(--cyan), var(--green));
  padding: 7px 12px;
  border-radius: 999px;
  font-weight: 1000;
  margin-bottom: 14px;
}

.timeline-item h3 {
  margin-bottom: 10px;
}

.audience-grid {
  grid-template-columns: repeat(3, 1fr);
}

.audience-grid div {
  font-weight: 900;
}

.scary {
  text-align: center;
  border-top: 1px solid var(--danger-border);
  border-bottom: 1px solid var(--danger-border);
  background:
    radial-gradient(circle at center, rgba(255,0,60,0.16), transparent 45%),
    rgba(2,5,10,0.75);
}

.scary h2 {
  max-width: 950px;
  margin: auto;
  margin-bottom: 18px;
  color: #fff;
  text-shadow: 0 0 28px rgba(255,0,60,0.25);
}

.scary p {
  max-width: 780px;
  margin: auto;
}

.register {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 30px;
  align-items: center;
  background:
    radial-gradient(circle at 10% 20%, rgba(0,245,255,0.14), transparent 30%),
    radial-gradient(circle at 85% 80%, rgba(53,255,141,0.12), transparent 32%);
}

.register h2 {
  margin-bottom: 14px;
}

footer {
  padding: 28px 6%;
  text-align: center;
  color: var(--muted);
  background: rgba(2,5,10,0.95);
  border-top: 1px solid var(--border);
}

@keyframes floatY {
  0%, 100% {
    transform: translateY(0);
  }

  50% {
    transform: translateY(-10px);
  }
}

@keyframes blink {
  0%, 45% {
    opacity: 1;
  }

  46%, 100% {
    opacity: 0;
  }
}

@keyframes glitch {
  0%, 100% {
    opacity: 0.25;
    transform: translateX(0);
  }

  10% {
    opacity: 0.8;
    transform: translateX(3px);
  }

  13% {
    transform: translateX(-3px);
  }

  16% {
    transform: translateX(0);
  }
}

@keyframes skullPulse {
  0%, 100% {
    opacity: 0.055;
  }

  50% {
    opacity: 0.12;
  }
}

@media (max-width: 1100px) {
  .hero,
  .register {
    grid-template-columns: 1fr;
  }

  .skill-grid,
  .demo-grid,
  .audience-grid,
  .timeline {
    grid-template-columns: repeat(2, 1fr);
  }

  .workshop-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 760px) {
  .menu-btn {
    display: block;
  }

  nav {
    position: absolute;
    top: 74px;
    left: 6%;
    right: 6%;
    display: none;
    flex-direction: column;
    align-items: flex-start;
    padding: 18px;
    border-radius: 18px;
    background: rgba(2,5,10,0.98);
    border: 1px solid var(--border);
  }

  nav.open {
    display: flex;
  }

  .hero {
    padding-top: 78px;
  }

  .hero::before {
    top: 28px;
    font-size: 10px;
    letter-spacing: 0.12em;
  }

  .trust-row,
  .skill-grid,
  .demo-grid,
  .audience-grid,
  .timeline {
    grid-template-columns: 1fr;
  }

  .terminal-card {
    transform: none;
  }

  .floating-badge {
    position: static;
    display: inline-block;
    margin: 8px 8px 0 0;
  }

  .ai-skull {
    font-size: 90px;
    right: 10px;
    top: 120px;
  }

  .section::before {
    display: none;
  }
}
